# This message is used as an example of a sensor message. It's used for tests as well.

# Each sensor has a 'header', where the 'frame_id' represents the place where the
# sensor is located at origin
std_msgs/Header header

# The value can be of any type, even made up of other message types.
int64 value